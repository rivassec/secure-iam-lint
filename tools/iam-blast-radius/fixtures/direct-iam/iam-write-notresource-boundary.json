{
  "name": "Direct IAM write scoped by NotResource (boundary)",
  "note": "IAM-504 fixture matrix: DIRECT-IAM-ADMIN with a NotResource scope. Allow iam:PutUserPolicy + iam:AttachRolePolicy on NotResource (everything EXCEPT one protected role) still grants direct self-administration; the NotResource 'allow everything except' shape is broad, so WILDCARD-RESOURCE fires too. exactFindingIds is the pre-correlation RULE set (rules.js): DIRECT-IAM-ADMIN + WILDCARD-RESOURCE. Correlation: IAM-201 folds the same-statement WILDCARD-RESOURCE into DIRECT-IAM-ADMIN, then IAM-705 folds the generic DIRECT-IAM-ADMIN (carrying that nested WILDCARD-RESOURCE) into the highest-priority co-located specific primitive (ATTACH-POLICY) - nothing lost. Provides the DIRECT-IAM-ADMIN NotResource cell of the per-rule matrix (witnessed via the subsumed snapshot).",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Sid": "IamWriteExceptProtected",
        "Effect": "Allow",
        "Action": ["iam:PutUserPolicy", "iam:AttachRolePolicy"],
        "NotResource": ["arn:aws:iam::111122223333:role/protected-break-glass"]
      }
    ]
  },
  "expect": {
    "valid": true,
    "exactFindingIds": ["WILDCARD-RESOURCE", "DIRECT-IAM-ADMIN"],
    "findingIds": ["ATTACH-POLICY", "PUT-INLINE-POLICY"],
    "notFindingIds": ["DATA-EXFIL", "DESTRUCTIVE-ACTION"],
    "compound": {
      "primaryId": "ATTACH-POLICY",
      "subsumes": [
        { "id": "DIRECT-IAM-ADMIN", "statementIndex": 0 },
        { "id": "WILDCARD-RESOURCE", "statementIndex": 0 }
      ],
      "independentFindings": [{ "id": "PUT-INLINE-POLICY", "statementIndex": 0 }],
      "note": "IAM-705 + IAM-201 nested subsumption: the generic DIRECT-IAM-ADMIN and the WILDCARD-RESOURCE it had absorbed both ride into the ATTACH-POLICY primary's subsumed[]. PUT-INLINE-POLICY stays an independent top-level technique.",
      "riskFactors": [
        { "key": "direct-iam-admin", "present": true },
        { "key": "resource-wildcard", "present": true }
      ]
    }
  }
}
