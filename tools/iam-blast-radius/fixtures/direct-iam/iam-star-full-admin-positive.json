{
  "name": "iam:* on Resource * (positive, multi-finding)",
  "note": "iam:* is a service wildcard (WILDCARD-ACTION, high), a direct-IAM-admin grant (DIRECT-IAM-ADMIN, high per the IAM-102 severity model), and non-read on Resource \"*\" (WILDCARD-RESOURCE). Exercises the intended triple.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      { "Sid": "IamAdmin", "Effect": "Allow", "Action": "iam:*", "Resource": "*" }
    ]
  },
  "expect": {
    "valid": true,
    "exactFindingIds": ["WILDCARD-ACTION", "WILDCARD-RESOURCE", "DIRECT-IAM-ADMIN"],
    "findingIds": ["WILDCARD-ACTION", "WILDCARD-RESOURCE", "DIRECT-IAM-ADMIN"],
    "notFindingIds": [],
    "summary": {
      "counts": { "privEscPaths": 5, "roleAssumption": 0, "sensitiveData": 0, "broadResource": 1 },
      "highestRisk": { "id": "ATTACH-POLICY", "severity": "high", "path": "Principal -> iam:Attach*Policy -> attacker-chosen managed policy" }
    }
  }
}
