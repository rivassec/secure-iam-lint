{
  "name": "Direct IAM administration (positive)",
  "note": "iam:PutUserPolicy + iam:AttachRolePolicy + iam:CreatePolicyVersion on a scoped IAM resource. Isolates DIRECT-IAM-ADMIN: resource is not \"*\" so no WILDCARD-RESOURCE, and these are non-destructive writes.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Sid": "SelfServiceIam",
        "Effect": "Allow",
        "Action": ["iam:PutUserPolicy", "iam:AttachRolePolicy", "iam:CreatePolicyVersion"],
        "Resource": ["arn:aws:iam::111122223333:user/${aws:username}", "arn:aws:iam::111122223333:role/app-*", "arn:aws:iam::111122223333:policy/app-*"]
      }
    ]
  },
  "expect": { "valid": true, "exactFindingIds": ["DIRECT-IAM-ADMIN"], "findingIds": ["DIRECT-IAM-ADMIN"], "notFindingIds": ["WILDCARD-ACTION", "WILDCARD-RESOURCE"] }
}
