{
  "name": "Secret / KMS read (positive)",
  "note": "secretsmanager:GetSecretValue + kms:Decrypt are exfil-capable reads and fire DATA-EXFIL regardless of resource breadth (reading a secret is the exfil act). Scoped resources isolate DATA-EXFIL from WILDCARD-RESOURCE.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Sid": "ReadSecrets",
        "Effect": "Allow",
        "Action": ["secretsmanager:GetSecretValue", "kms:Decrypt"],
        "Resource": ["arn:aws:secretsmanager:us-east-1:111122223333:secret:app/*", "arn:aws:kms:us-east-1:111122223333:key/abcd-1234"]
      }
    ]
  },
  "expect": { "valid": true, "exactFindingIds": ["DATA-EXFIL"], "findingIds": ["DATA-EXFIL"], "notFindingIds": ["WILDCARD-RESOURCE", "WILDCARD-ACTION", "DESTRUCTIVE-ACTION"] }
}
