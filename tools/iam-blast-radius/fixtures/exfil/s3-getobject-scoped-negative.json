{
  "name": "s3:GetObject scoped (negative)",
  "note": "s3:GetObject on a single scoped bucket prefix is routine and must NOT fire DATA-EXFIL: the bulk-read rule requires a broad resource scope. Guards the \"on *\" boundary of the exfil rule.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      { "Sid": "ReadOneBucket", "Effect": "Allow", "Action": "s3:GetObject", "Resource": "arn:aws:s3:::reports-bucket/*" }
    ]
  },
  "expect": { "valid": true, "exactFindingIds": [], "findingIds": [], "notFindingIds": ["DATA-EXFIL", "WILDCARD-RESOURCE"] }
}
