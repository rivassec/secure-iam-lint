{
  "name": "Allow with no Deny is not a deny (negative)",
  "note": "No Deny statement at all. decide() must report allowed-by-policy for the granted action and not-granted-by-policy for an action the policy never mentions - never an explicit-deny.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      { "Sid": "AllowRead", "Effect": "Allow", "Action": ["s3:GetObject", "s3:ListBucket"], "Resource": "arn:aws:s3:::data/*" }
    ]
  },
  "expect": {
    "valid": true,
    "decide": [
      { "action": "s3:GetObject", "resource": "arn:aws:s3:::data/report.csv", "decision": "allowed-by-policy" },
      { "action": "s3:DeleteObject", "resource": "arn:aws:s3:::data/report.csv", "decision": "not-granted-by-policy" }
    ]
  }
}
