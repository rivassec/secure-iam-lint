{
  "name": "Conditional Deny -> context-required denies edge",
  "note": "A Deny gated by a Condition may not fire at runtime, so its edge is context-required, NOT blocked-by-deny (overstating a false block is a truthfulness harm).",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Sid": "DenyUnlessMfa",
        "Effect": "Deny",
        "Action": ["s3:DeleteObject"],
        "Resource": "*",
        "Condition": { "Bool": { "aws:MultiFactorAuthPresent": "false" } }
      }
    ]
  },
  "expect": {
    "valid": true,
    "findingIds": [],
    "exactGraphEdges": [
      { "from": "principal", "to": "actiongroup:deny#0", "type": "denies", "certainty": "context-required" }
    ]
  }
}
