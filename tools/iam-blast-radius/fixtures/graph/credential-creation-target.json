{
  "name": "CreateAccessKey -> can-modify a target Principal node",
  "note": "Credential creation for another principal produces a can-modify edge from the policy subject to a synthetic target-Principal node (the credential recipient).",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      { "Sid": "MintKeys", "Effect": "Allow", "Action": "iam:CreateAccessKey", "Resource": "arn:aws:iam::111122223333:user/*" }
    ]
  },
  "expect": {
    "valid": true,
    "findingIds": ["CREDENTIAL-CREATION"],
    "graphEdges": [
      { "from": "principal", "to": "principal:credential-target", "type": "can-modify", "certainty": "confirmed-by-context" }
    ]
  }
}
