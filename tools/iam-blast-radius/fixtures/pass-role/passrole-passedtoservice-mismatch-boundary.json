{
  "name": "PassRole pinned to a different service (boundary, NOT an escalation)",
  "note": "iam:PassRole is pinned via iam:PassedToService to ec2, but the only service-execution action granted is lambda:CreateFunction. The PassRole cannot feed Lambda, so no path is flagged. Verifies iam:PassedToService is respected.",
  "policy": {
    "Version": "2012-10-17",
    "Statement": [
      {
        "Sid": "PassToEc2Only",
        "Effect": "Allow",
        "Action": "iam:PassRole",
        "Resource": "arn:aws:iam::111122223333:role/ec2-*",
        "Condition": { "StringEquals": { "iam:PassedToService": "ec2.amazonaws.com" } }
      },
      {
        "Sid": "CreateLambda",
        "Effect": "Allow",
        "Action": ["lambda:CreateFunction"],
        "Resource": "*"
      }
    ]
  },
  "expect": { "valid": true, "findingIds": [], "notFindingIds": ["PASSROLE-LAMBDA", "PASSROLE-EC2", "PASSROLE-SERVICE"] }
}
