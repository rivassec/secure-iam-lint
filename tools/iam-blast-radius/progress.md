# Progress ledger - IAM Blast Radius

Updated by the Ralph loop. Status: pending | in-progress | accepted |
human-review.

| Story | Title | Status | Iterations | Blocking findings left | Notes |
|---|---|---|---|---|---|
| IAM-001 | Input validation + guards | accepted | 1 | 0 | engine/validate.js + tests; 23 tests pass; gates clean; critics returned zero blocking findings, arbiter accepted on iteration 1 |
| IAM-002 | Parser + normalized model | accepted | 1 | 0 | engine/parse.js + engine/model.js + tests; 33 parse tests pass (57 total across suite); no-network + no-unsafe-dom gates clean; critics returned zero blocking findings, arbiter accepted on iteration 1 |
| IAM-003 | Evaluator semantics | accepted | 1 | 0 | engine/evaluator.js + tests; evaluate()/decide() with explicit-Deny precedence, NotAction/NotResource inversion, conditional-deny never a false deny, capability-not-effective caveat on every output; 89 tests pass (32 new); no-network + no-unsafe-dom gates clean; ReDoS-safe linear glob matcher; critics returned zero blocking findings, arbiter accepted on iteration 1 |
| IAM-004 | Risk-rule catalog | accepted | 1 | 0 | engine/rules.js + tests + fixtures; analyzeRules()/analyzeRulesFromText() emit canonical findings for WILDCARD-ACTION, WILDCARD-RESOURCE, DIRECT-IAM-ADMIN, DATA-EXFIL, DESTRUCTIVE-ACTION, DETECTION-IMPAIRMENT, NOTACTION-ALLOW; Allow-only, deterministic + deep-frozen, ReDoS-safe glob, capability-not-effective limit on every finding; wildcard/ produce expected findings, safe/ produce none; 45 new tests (149 total) pass; no-network + no-unsafe-dom gates clean; new fixture dirs direct-iam/destructive/exfil/detection/notaction-allow (positive+negative+boundary each); critics returned zero blocking findings, arbiter accepted on iteration 1 |
| IAM-005 | Escalation engine | accepted | 1 | 0 | engine/escalation.js + tests/escalation.test.js + fixtures; analyzeEscalations()/analyzeEscalationsFromText() emit canonical findings for 9 escalation paths: PASSROLE-LAMBDA, PASSROLE-EC2, PASSROLE-SERVICE (ecs/glue/cloudformation/sagemaker/codebuild/datapipeline), POLICY-VERSION, ATTACH-POLICY, PUT-INLINE-POLICY, TRUST-POLICY-MODIFY, CREDENTIAL-CREATION, ASSUME-ROLE-EXPANSION; PassRole respects Resource + iam:PassedToService and never flags PassRole alone; Allow-only, deterministic + deep-frozen, ReDoS-safe glob; every finding carries capability-not-effective + target-unknown (targetPermissions:'unknown', never inferred) limit plus escalation{technique,service,requiredActions} and evidence[]; new fixture dirs assume-role/policy-version/attach-policy/credential-creation (+ pass-role boundary), positive+negative+boundary each; 48 new tests (197 total) pass; no-network + no-unsafe-dom gates clean |
| IAM-006 | Graph model builder | accepted | 1 | 0 | engine/graph.js + tests/graph.test.js + fixtures/graph/; buildGraph(model,findings)/buildGraphFromText() produce a pure data {nodes,edges,truncated,limits} structure (no DOM/SVG). Nodes use the architecture vocab (Principal/Policy/Role/Service/ActionGroup/Resource/DataStore + target-Principal); edges use allows/denies/can-assume/can-pass/can-modify/can-execute-as/can-read/can-write/can-destroy; every edge carries a certainty class (confirmed-by-context/conditionally-reachable/potentially-reachable/blocked-by-deny/unknown-incomplete-context) + non-empty evidence[] (statementSid/index/actions/resources/condition). Rule + escalation findings map deterministically to edges; same-policy Deny statements become blocked-by-deny (unconditional) / conditionally-reachable (conditioned) `denies` edges; duplicate relationships merge into one edge keeping the strongest certainty + all evidence; node count bounded by GRAPH_LIMITS.MAX_NODES (truncates, never throws). Deterministic ordering + deep-frozen; hostile ARNs/Sids ride through as inert node ids only; no proto pollution. Added graphEdges/exactGraphEdges expectations to pass-role + wildcard fixtures and a new graph/ fixture dir (deny-blocked/conditional-deny/assume-role-potential/trust-modify-conditioned/credential-creation/safe-empty/malformed). 91 new tests (308 total) pass; no-network + no-unsafe-dom gates clean. |
| IAM-007 | UI shell + worker | accepted | 1 | 0 | app.js (DOM controller) + worker.js (module Web Worker) + engine/analyze.js (pure orchestration, node-testable) + engine/report.js (JSON/MD export). Findings render as an accessible table (caption + th[scope=col]) built with createElement+textContent only; hostile SIDs/ARNs ride through as inert text/data. Module worker with sync fallback + wall-clock watchdog (terminates on overrun). Clear button + pagehide wipe in-memory state; no storage of policy content. Export buttons wired via Blob download (HTML export deferred to IAM-008). Graph section shows a textual node/edge summary placeholder (SVG render is IAM-008). Zero inline style/script/on-handlers; all events via addEventListener; export buttons disabled until a run. 33 new node --test cases (341 total) pass; gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean. Added Playwright spec (tests/e2e/ui-shell.spec.js: inert-render/no-egress/keyboard/clear/no-storage) + playwright.config.js + .github/workflows/iam-blast-radius-ci.yml (pinned action SHAs). Browser matrix + security/privacy/UX-a11y critics are CI's job (not run here). Critics returned zero blocking findings; arbiter accepted on iteration 1. |
| IAM-108 | Phase 2: input caps (DoS) - reject oversized policies | accepted | 1 | 0 | validate() enforces LIMITS.MAX_STATEMENTS (1000) and rejects with TOO_MANY_STATEMENTS before any analysis (analyze() returns ok:false, tab never hangs); graph builder bounds node/edge count via GRAPH_LIMITS and returns truncated:true with the findings table staying authoritative. New adversarial fixture fixtures/adversarial/oversized-statements.json (1001 statements) yields validate() error + analyze() ok:false + no hang; bounded-edge coverage in tests/graph.test.js; input-cap coverage in tests/analyze.test.js; existing safe/wildcard fixtures unaffected; limits are documented constants with user-clear error messages. Full node --test suite green; reliability critic >=95; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-103 | Phase 2: wording precision fixes (correctness of security claims) | accepted | 1 | 0 | Wording-precision pass landed in shipped engine (engine/escalation.js + engine/graph.js): ASSUME-ROLE-EXPANSION now says "roles across arbitrary AWS accounts whose trust policies permit this principal" (arn:aws:iam::*:role/* spans all accounts); PassRole family reworded to "does not use iam:PassedToService to restrict which supported AWS services may receive the role" (AWS still enforces service support); PassRole+EC2/service downgraded from definitive "gaining the role permissions" to "potentially obtaining execution under the passed role credentials"; kms:Decrypt split into a distinct Decryption capability finding (no longer "reads secret material", no longer lumped with Secrets Manager); wildcard rule drops the "non-read action" classification for neutral "Resource * leaves this action broadly resource-scoped". Affected fixtures' expected text/ids updated; full node --test suite green; IAM-semantics critic 100 (claims accurate vs AWS docs); critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-102 | Phase 2: severity remodel (reserve critical for compound escalation) | accepted | 1 | 0 | Deterministic, documented severity model landed in shipped engine (engine/escalation.js + engine/rules.js): `critical` is reserved for compound escalation paths that plausibly cross a privilege boundary - PassRole+lambda:CreateFunction, PassRole+ec2:RunInstances and service peers, and AssumeRole whose resource scope is effectively all roles (`*:role/*` / `role/*`); standalone wildcard scope, broad kms:Decrypt, secrets access, and direct-IAM single actions (attach/put-inline-policy-to-self) stay `high`; no severity is asserted higher than the evidence supports. Every affected fixture's expected severity updated; severity-model coverage added across tests/escalation.test.js, tests/rules.test.js, and tests/report.test.js (standalone wildcard-action caps at HIGH; compound PassRole+service path is CRITICAL). Full node --test suite green; IAM-semantics critic 100 (claims accurate vs AWS docs); critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-104 | Phase 2: split confidence into policy-evidence vs path-exploitability | accepted | 1 | 0 | Single `confidence` field replaced by two orthogonal signals across the shipped engine (engine/escalation.js, engine/rules.js, engine/graph.js, engine/analyze.js): every finding now carries `policyEvidence` (high\|medium\|low - how plainly the grant + scope sit in the policy) AND `pathExploitability` (high\|medium\|low - how reachable the privilege boundary is given unknown surrounding controls). Compound paths show evidence>=exploitability where appropriate (e.g. PassRole+lambda:CreateFunction -> policyEvidence high, both perms present; pathExploitability medium, privileged passable role + other controls unknown). Findings table + evidence panel render both signals; render-graph enriches both split certainty signals + the limit from the finding; JSON/MD export (engine/report.js) includes both. The capability-not-effective limit text is retained on every finding. Affected fixtures' expected fields updated; full node --test suite green (385 pass, 0 fail); reliability critic >=95; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-105 | Phase 2: compound-finding correlation + risk-factor checklist | accepted | 1 | 0 | New engine/correlate.js (pure, deterministic, deep-frozen) folds a subordinate WILDCARD-ACTION/WILDCARD-RESOURCE finding INTO the compound PassRole->service escalation path whose pass/exec statement it sits on (matched by evidence[].statementIndex), so the findings table shows ONE primary path row carrying a present/absent `riskFactors` checklist plus a `subsumed` sub-property (full prose preserved, nothing dropped) instead of duplicate wildcard rows. correlateFindings() wired into engine/analyze.js AFTER rules+escalation combine and BEFORE sortFindings; the graph is still built from the full pre-correlation set (a subsumed grant is still a real edge). escalation.js emits `riskFactors` on the passrole-service-execution finding only (iam:PassRole granted, each service exec action, PassRole resource wildcard-scoped, exec Resource:*, iam:PassedToService restriction present) - single-action primitives keep riskFactors:null. A wildcard finding on a statement NO compound path consumes stays an independent top-level row (proven by passrole-passedtoservice-stringnotequals-boundary: EC2 path subsumes wildcard@0+@2, blocked-Lambda wildcard@1 survives). report.js JSON dumps riskFactors+subsumed verbatim; MD renders a [x]/[ ] checklist + subsumed list. app.js renders a safe (createElement+textContent) `tr.finding-detail` row with the checklist + folded findings; styles.css adds the detail-row styling (no inline style/script). 16 new node --test cases (401 total, new tests/correlate.test.js + compound coverage in analyze/escalation/report suites) pass; gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean. Updated tests/e2e/ui-shell.spec.js (checklist rendered, no duplicate wildcard row) - browser matrix is CI's job. |
| IAM-106 | Phase 2: risk-summary header above the findings table | accepted | 1 | 0 | engine/analyze.js gains a pure, deterministic summarize()/SUMMARY_CATEGORIES projection folding findings into scannable counts (privilege-escalation paths, role-assumption capabilities, sensitive-data access, broad-resource grants) plus the single highest-risk escalation path in one line (id/severity/path); summarize() total equals the finding count, counts never exceed total (curated, not double-counted), and empty/null/undefined input is tolerated. app.js renders a `section.risk-summary` above the table via createElement+textContent only (aria-labelledby heading, `dl.risk-summary-counts`, highest-risk top line), styles.css adds risk-summary styling with no inline style/script; findings table stays authoritative. Annotated fixtures assert summary counts + highest-risk path match. Full node --test suite green (405 pass, 0 fail); scripts/csp_audit.py clean (no inline styles/style blocks/event handlers); e2e coverage in tests/e2e/ui-shell.spec.js (header renders above table with counts + highest-risk path; no path shown when no escalation). UX/a11y critic no serious/critical; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-101 | Phase 2: compact findings table (move prose to the evidence detail) | accepted | 1 | 0 | Findings table trimmed to scannable columns (Severity, Finding, Statement, Actions, Resources, split policy-evidence/path-exploitability); full why/limit/remediation prose + risk-factor checklist moved OUT of the row into a click-and-keyboard expandable per-row detail (extends the #evidence panel pattern) so no prose is lost and every row's detail is reachable without the graph; avg finding row height drops well under 120px (was ~650px from three wrapping prose columns); table stays the authoritative view and fully accessible without the graph. Safe DOM only (createElement+textContent, events via addEventListener, no inline style/script/on-handlers); full node --test suite green; scripts/csp_audit.py + gate:no-network + gate:no-unsafe-dom clean; e2e (tests/e2e/ui-shell.spec.js) updated for the compact layout (compact rows, detail expands via click+keyboard, prose reachable); UX/a11y critic no serious/critical; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-107 | Phase 2: graph encodes the privilege transition (unknown-privileges node) | accepted | 1 | 0 | Shipped in engine/graph.js + engine/render-graph.js: escalation paths chain THROUGH an intermediate `passable-role` pivot (principal --can-pass--> role:passable:{svc} [unknown privileges] --can-execute-as--> service:{svc}) instead of a direct principal->service spoke, so the graph shows the privilege transition, not just the service sequence. The pivot node carries the analyzer's KNOWN/UNKNOWN split (the two grants are known; the passed role's actual permissions are not - marker `unknownPrivileges`) and styles.css gives it a distinguished dashed warn-tinted box. render-graph.js lays the transition out left-to-right by hop-distance from the principal (pivot sits between principal and service). Edges keep evidence + certainty class; SVG text via textContent only, numeric geometry attrs only, no markup from input; deterministic (no Date.now()/Math.random()); findings table stays authoritative. Fixtures fixtures/graph/passrole-transition-unknown-privileges.json + wildcard/admin-star.json graphNote assert the pivot transition; coverage in tests/graph.test.js + tests/render-graph.test.js (pivot renders as distinguished unknown-privileges node, left-to-right column layout) + e2e tests/e2e/graph.spec.js (injection fixtures still inert, reduced-motion honored - browser matrix is CI's job). Full node --test suite green (413 pass, 0 fail); UX/a11y critic no serious/critical; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-008 | SVG graph + export | accepted | 1 | 0 | engine/render-graph.js is the only module that touches SVG/DOM; consumes the pure graph DATA from graph.js (IAM-006) and paints an inline SVG behind a rendering interface (computeLayout()/createGraphRenderer(doc)), keeping the data model rendering-agnostic (architecture invariant 7). Node/edge labels embed hostile policy strings (Sids/ARNs/condition values) written via textContent only; geometry attributes are finite numbers from our deterministic layout; class attributes come from a fixed CERTAINTY_CLASSES vocab (distinct style per certainty class, never blended); no markup ever built from input (threat-model T1). No network/storage; no eval/Function/inline handlers (events via addEventListener); deterministic (no Date.now()/Math.random()) - same graph -> identical geometry+order. Graph is progressive enhancement; the findings table stays the authoritative view; click AND keyboard open the evidence panel; reduced-motion honored. Export stays JSON/MD via report.js (toJSON/toMarkdown) - HTML export intentionally declined per threat-model T6 (downloaded .md/.json do not execute markup), superseding the IAM-007 "HTML export deferred" note. 21 new node --test cases (362 total, 17 in tests/render-graph.test.js against an injected minimal Document stub) pass; gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree. Added tests/e2e/graph.spec.js (inline-SVG render, per-certainty edge styles, SVG/HTML-injection inertness, click+keyboard evidence panel, reduced-motion, table-authoritative) - browser matrix is CI's job. Critics returned zero blocking findings; arbiter accepted on iteration 1. |
| IAM-201 | Phase 2.1: extend subsumption to same-statement capability findings + summary metric + grammar | accepted | 1 | 0 | engine/correlate.js generalized: a same-statement WILDCARD-RESOURCE scope amplifier is folded into the more-specific capability finding on that statement (fixed priority KMS-DECRYPT > DATA-EXFIL > DIRECT-IAM-ADMIN > DESTRUCTIVE-ACTION > DETECTION-IMPAIRMENT) as a risk factor rather than a duplicate top-level row (nothing lost; preserved in subsumed[] + risk factors); a standalone WILDCARD-RESOURCE with no more-specific capability on its statement stays independent, and a full-* WILDCARD-ACTION is never subsumed. KMS fixture now yields ONE primary KMS-DECRYPT finding with [x] kms:Decrypt granted / [x] Resource:* / [ ] kms:ViaService / [ ] kms:EncryptionContext risk factors (primaries drop 6->5). Risk-summary metric renamed 'Broad-resource grants' -> 'Standalone broad-resource findings' (engine/analyze.js, counts post-subsumption standalone wildcards only). Grammar fixed in engine/rules.js ('reads secret material' -> 'read secret material'). Full node --test suite green (415 pass, 0 fail); IAM-semantics critic 100; critics returned zero blocking findings, arbiter accepted on iteration 1. |
| IAM-301 | Phase 3: author + validate the negative regression corpus (does-not-fire truth) | accepted | 1 | 0 | Data + rationale only, no engine files changed. Authored 16 fixtures under fixtures/negative/ (>=15 required), each carrying policy + expect (findingIds it MUST produce, notFindingIds/severities it MUST NOT overstate) + a `rationale` citing the AWS behavior that makes the expectation correct (every fixture has rationale). Corpus covers the required does-not-fire cases: cross-statement compound still detected (compound-split-across-statements), PassRole pinned by iam:PassedToService to a non-matching service not flagged (passedtoservice-service-mismatch), PassRole scoped to one specific harmless role not flagged broad (passrole-specific-role-not-broad), explicit Deny suppresses (explicit-deny-suppresses-exfil), NotAction/NotResource semantics (notaction-allow-excludes-iam, notresource-deny-fences-exfil), Condition narrowing lowers/acknowledges (condition-sourceip-narrows-exfil, kms-decrypt-viaservice-narrows), permissions-boundary-shaped no escalation, resource-based/trust policy no identity escalation (resource-based-trust-policy-no-escalation), read-only-admin wildcard broad-not-critical (read-only-admin-wildcard-not-escalation), AWS-required Resource:* not penalized (aws-required-wildcard-resource-not-penalized), single-account AssumeRole lower than all-accounts (assumerole-single-account-lower-than-all-accounts), UpdateFunctionCode without PassRole not an escalation (update-function-code-without-passrole), PassRole without workload primitive not escalation (passrole-without-workload-primitive), two chains sharing one PassRole (two-chains-share-one-passrole). Fixtures inert until IAM-302 wires them; existing full node --test suite stays green; IAM-semantics critic 100 (every expected outcome correct per real AWS semantics); critics returned zero blocking findings, arbiter accepted on iteration 1 (max 4). |
| IAM-202 | Phase 2.1: graph edge certainty vocabulary + can-decrypt edge type | accepted | 1 | 0 | Shipped in the graph engine: PassRole->service transition edges downgraded from `confirmed-by-context` to `policy-supported` (grants are literally in the policy, but the transition still needs an out-of-scope precondition - a usable target role / trust config); AssumeRole stays `potentially-reachable`. Certainty vocabulary settled as confirmed-by-policy / policy-supported / potentially-reachable / context-required / blocked-by-deny / unknown-incomplete-context. New distinct edge type `can-decrypt` for kms:Decrypt (split out of `can-read`), human label "can decrypt ciphertext"; edge-type ontology + render-graph legend/styles give it a visually distinct treatment from can-read. Graph fixtures + tests updated for the new certainty values and edge type (fixtures/graph/kms-decrypt-can-decrypt-edge.json + passrole-transition-unknown-privileges.json + wildcard/admin-star.json; coverage in tests/graph.test.js + tests/render-graph.test.js + e2e tests/e2e/graph.spec.js); SVG text via textContent only, injection fixtures stay inert. Full node --test suite green (420 pass, 0 fail); UX/a11y critic no serious/critical; critics returned zero blocking findings, arbiter accepted on iteration 1 (max 4). |
| IAM-401 | Phase 4: path-focused, lane-grouped attack-path graph | accepted | 1 | 0 | graph.js assigns each edge/path to a semantic lane (PRIVILEGE ESCALATION / IDENTITY EXPANSION / DATA ACCESS / SCOPE); render-graph.js draws one labeled section per non-empty lane (empty lanes omitted) so escalation paths read top-to-bottom and don't compete with data-access/scope nodes. Preserves all earlier-phase behavior: the 'Passable Role [unknown privileges]' transition pivot, certainty styles (policy-supported/potentially-reachable/etc.), can-decrypt vs can-read edge types, per-edge evidence on select, reduced-motion, and SVG text via textContent (injection-inert). Findings table stays authoritative; graph remains progressive-enhancement + keyboard-accessible with an equivalent accessible representation; deterministic layout/ordering; existing max-node cap enforced. New fixture fixtures/graph/lane-grouped-multi-category.json exercises all four lanes at once; coverage in tests/graph.test.js + tests/render-graph.test.js + e2e tests/e2e/graph.spec.js (lane sections render, unknown-privileges pivot inside its lane, injection fixtures stay inert, reduced-motion honored). Full node --test suite green (448 pass, 0 fail); UX/a11y + security critics no serious/critical; critics returned zero blocking findings, arbiter accepted on iteration 1 (max 5). No remaining blockers. |
| IAM-302 | Phase 3: wire the negative corpus + fix the engine to match the validated truth | accepted | 1 | 0 | New tests/negative.test.js runs all 16 fixtures/negative/ frozen "does-not-fire truth" fixtures through the full analyze() pipeline and asserts the authoritative findings table against each fixture's negativeExpect contract (mustFind[] present, mustNotFind[] absent, maxSeverity{} not overstated, maxPathExploitability{} not overstated); no fixture expectation was weakened - IAM-301's corpus + rationale stayed frozen and the engine was fixed to match. Engine fixes landed in the shipped tree (content/tools/iam-blast-radius/engine/): escalation.js honors iam:PassedToService service-match before flagging a PassRole path, does not flag PassRole scoped to a specific role ARN or PassRole-without-workload-primitive, and keeps single-account AssumeRole below all-accounts (*:role/*); rules.js treats AWS-required Resource:* actions as not-an-avoidable-wildcard where determinable and keeps read-only-admin wildcards broad-but-not-escalation; analyze.js pipeline preserves cross-statement compound detection, explicit-Deny suppression, and NotAction/NotResource semantics. All Phase-1/2/2.1 behavior preserved; determinism, safe DOM, zero egress intact. Full node --test suite green (439 pass, 0 fail; negative suite included); IAM-semantics critic 100 (no false allow/deny, no overstated severity); reliability critic >=95; critics returned zero blocking findings, arbiter accepted on iteration 1 (max 6). |
| IAM-502 | Phase 5: coverage summary above findings + in every export | accepted | 1 | 0 | New engine/coverage.js (pure, deterministic, deep-frozen) enrichCoverage() folds the IAM-501 family-gate coverage into a compact analysis-coverage summary (SUPERSET: all IAM-501 fields preserved + a `summary` sub-object): detected/effective family (+override), statements accepted/rejected (fail-closed => 0 accepted on a blocked shape), unrecognizedActions/unsupportedConditions (reserved empty arrays for IAM-507/506), unsupportedElements derived from blockingCodes (NotPrincipal + its exact JSON path), missingLayers (the AWS evaluation layers this single document did NOT cover - the concrete form of "not effective permissions"), graph complete/truncated, versions{buildSha,ruleVersion,catalogVersion}, stable machine-readable codes, and an `incomplete` flag (blocked OR any unsupported element/action/condition). analyze.js attaches the enriched coverage on both the success and blocked paths. app.js renders a COMPACT #coverage panel in its own <section> ABOVE the findings section (precedes findings in visual + DOM order), safe DOM only (createElement+textContent; family tokens/JSON paths/action names ride through as inert text), taking a prominent .coverage-incomplete warning state ("unsupported does NOT mean safe") whenever coverage is incomplete; index.html adds the section, styles.css the panel styling (no inline style/script). The zero-findings wording flips automatically via noFindingsMessage(): "No findings in the supported subset - unsupported elements prevent a complete conclusion" when incomplete, the plain "No blast-radius findings" message otherwise - shared by app.js + report.js. report.js JSON carries coverage.summary (with codes) verbatim; Markdown gains the enriched "Analysis coverage" section (statements accepted/rejected, unrecognized actions, unsupported conditions/elements, missing layers, graph state, build SHA + rule + catalog version, "unsupported does NOT mean safe") and the flipped zero-findings line. New tests/coverage.test.js (13 cases: superset preservation, supported/blocked/NotPrincipal summaries, missing-layer + override bookkeeping, graph-truncation reflection, determinism, wording flip, JSON+MD export contents) => full node --test suite green (483 pass, 0 fail); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree. Updated tests/e2e/ui-shell.spec.js (coverage panel precedes findings in DOM order, names family+versions, warning state on unsupported shape, Clear wipes it) - browser matrix is CI's job. noindex kept; no SEO/sitemap change (that is IAM-509). Critics returned zero blocking findings, arbiter accepted on iteration 1; no remaining blockers. |
| IAM-501 | Phase 5: policy-family model with auto-detect + fail-closed on unsupported shapes | accepted | 1 | 0 | New engine/family.js (pure, deterministic, deep-frozen) classifies the policy family from SHAPE - identity (no Principal/NotPrincipal), resource (all statements name a Principal), role-trust (all-Principal + only sts:AssumeRole* + no Resource), ambiguous (mixed Principal presence) - and produces a coverage state. AUTO-DETECT is the default (paste-and-go preserved: identity policies analyze exactly as before); an OPTIONAL manual family override is honored via analyze(text,{family}) -> worker message -> #policy-family selector. FAIL CLOSED: analyze() calls detectFamily() AFTER modelFromText and BEFORE any rule evaluation; on a shape the engine does not model it returns a BLOCKING COVERAGE STATE (ok:true, zero findings, empty graph, coverage.blocked=true) carrying machine-readable codes + exact JSON paths (UNSUPPORTED_POLICY_FAMILY @Statement[i].Principal, UNSUPPORTED_NOTPRINCIPAL @Statement[i].NotPrincipal, AMBIGUOUS_POLICY_SHAPE @Statement[i], OVERRIDE_SHAPE_MISMATCH). This also stops the identity WILDCARD-* rules from overstating risk on a resource/bucket policy. model.js now models Principal and NotPrincipal as DISTINCT, mutually-exclusive elements (new statement.notPrincipal field; both-in-one-statement is a hard PRINCIPAL_AND_NOTPRINCIPAL schema error @Statement[i]); the 4 fixtures carrying an expected model gained notPrincipal:null. Every export records family + coverage: report.js toJSON adds family + coverage (codes+paths), toMarkdown adds an "Analysis coverage" section. app.js renders a warning-styled blocking notice ("unsupported does NOT mean safe") in place of the findings table when blocked, keeps export enabled, and resets the override on Clear; index.html adds the CSP-clean family <select> (Auto-detect default) + styles.css. The two pre-existing Principal-bearing fixtures (assume-role/trust-policy-not-escalation, negative/resource-based-trust-policy-no-escalation) now fail closed via an EXPLICIT coverage state (still ok:true, still zero identity findings - frozen negative-corpus contract preserved). New fixtures/family/ (6: identity/resource/role-trust/notprincipal/mixed/both-exclusive) + new tests/family.test.js (22 cases: coverage contracts, distinct NotPrincipal element + exact path, classification per AWS grammar, override behavior, identity auto-detect across categories, export records family+coverage). Full node --test suite green (470 pass, 0 fail); gate:no-network + gate:no-unsafe-dom clean on the shipped tree; CSP-clean HTML (no inline style/script/on-handlers). Updated tests/e2e/ui-shell.spec.js (override selector present, resource/NotPrincipal blocking notice, override-to-unmodeled-family blocks) - browser matrix is CI's job. |
| IAM-503 | Phase 5: single-flight worker + fail-closed execution boundary | accepted | 1 | 0 | app.js only (no engine/HTML changes): reworked the analysis dispatch into a SINGLE-FLIGHT model. Module-level monotonic jobSeq + currentJob{id,worker,watchdog}; a new submission calls invalidateCurrentJob() (clears the prior watchdog + terminate()s the prior worker) BEFORE starting, so the newer job always wins and a stale (older) worker result can never overwrite it. Each worker message is accepted only when it is not stale (isStale() = currentJob moved on OR jobSeq advanced) AND carries THIS job's id AND a result object with a boolean ok; otherwise it fails closed. resetOutputForNewJob() clears findings/coverage/graph/evidence + drops state.lastAnalysis + disables export at job start so a superseded/failed run never leaves stale results on screen. FAIL-CLOSED BOUNDARY (the story's core): runSync() (main-thread analyze) is now reachable ONLY when `new Worker(...)` throws or Worker is undefined BEFORE analysis starts (architecture invariant 5). A worker crash (error event), runtime error, malformed/unreadable message (messageerror), or post-dispatch watchdog expiry (WATCHDOG_MS=8000) now calls failClosed() -> renders a bounded WORKER_FAILED error + status, NEVER re-running the engine on the hostile policy text on the main thread (the old error handler's runSync() fallback was the exact anti-pattern this story removes). Busy state exposed via setBusy() -> aria-busy on #findings + #analyze-btn (true at dispatch, false the instant a job settles - success/failure/crash/timeout); Clear + pagehide invalidate any in-flight job and clear busy. styles.css adds .findings[aria-busy="true"] dim + #analyze-btn[aria-busy="true"] progress cursor (external CSS, CSP-clean). New tests/e2e/single-flight.spec.js (7 specs, chromium/firefox/webkit via existing playwright.config.js): rapid double-submit -> newer result wins + no stale trace + single table; worker-crash / watchdog-timeout / malformed-message all fail closed with a bounded error AND no findings table/risk-summary (proof the engine did not reprocess on the main thread - stand-in workers via page.route carry no engine, so a table could only appear from an illegal main-thread run); aria-busy true-while-in-flight -> false-on-settle + status announcements (Analyzing / Analysis complete / Analysis failed / time budget). Full node --test suite green (483 pass, 0 fail - app.js is exercised only by e2e, engine untouched); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree. Playwright browser matrix is CI's job (specs updated, not run here). |
| IAM-504 | Phase 5: per-rule fixture matrix, explainable evidence, privacy invariants as release gates | accepted | 1 | 0 | Three deliverables landed. (1) PER-RULE FIXTURE MATRIX: new tests/fixture-matrix.test.js is a gate that requires every rule (8 rules.js + 9 escalation.js ids) to have a fixture for each semantically-applicable kind (positive/negative/explicit-Deny/condition/notAction/notResource/hostile) with a documented APPLICABILITY table (notAction/notResource excluded where they are not a realistic, engine-detectable expression of the rule); coverage is DERIVED from real analyze() output so a mislabeled fixture cannot mask a gap, and it also guards that APPLICABILITY tracks the live catalog. Filled gaps with 10 new fixtures: direct-iam/iam-write-notresource-boundary, exfil/kms-encrypt-only-negative, exfil/kms-decrypt-notresource-conditional-deny-boundary, destructive/delete-conditioned-notresource-boundary, detection/stop-telemetry-conditioned-notresource-deny-boundary, notaction-allow/allow-notaction-conditioned-boundary, pass-role/passrole-multiservice-conditional-deny-boundary, attach-policy/attach-conditioned-boundary, and two adversarial/hostile-strings-{capability-rules,escalation-paths} witnesses (hostile Sids/ARNs across all 17 ids, each riding through as inert data). deterministic-export asserted as a global cell. (2) EXPLAINABLE EVIDENCE: analyze.js stamps policyFamily on every finding (and every subsumed sub-finding) - the one missing member of the evidence set (policy family + statement index/Sid + normalized action/resource + condition + rule id + split certainty + limitation); report.js JSON carries it verbatim and Markdown names it per finding. New tests/evidence.test.js asserts the full set per finding across the whole corpus + in the JSON export. (3) PRIVACY INVARIANTS AS GATES: new tests/privacy-invariants.test.js (source+engine layer) - shipped JS has no network API, no web-storage/IndexedDB/cookie WRITE, no URL/history/window.name write; failed analysis never leaks policy text into error payloads; exports carry caveat+coverage+catalogVersion+buildSHA; hostile HTML/SVG/MD/Unicode inert. Hardening found + fixed a real Markdown structural-injection: report.js now runs interpolated policy-derived values (Sid/ARN/action/subsumed) through mdSafe() collapsing CR/LF + C0/C1 + U+2028/2029 to a space so a newline-laden Sid cannot forge a heading/bullet/quote in a downloaded .md (JSON stays byte-verbatim). New tests/e2e/privacy-invariants.spec.js (browser layer): zero late network, no storage writes, no policy text in URL/window.name, Clear + pagehide wipe, and worker/node PARITY (browser worker findings == Node engine module). app.js onPageHide now runs the full clearAnalysis(false) teardown (was in-memory-only) so nothing policy-derived lingers in the DOM on bfcache restore. Full node --test suite green (535 pass, 0 fail; +52 from new fixtures + 3 new suites); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree. Playwright browser matrix is CI's job (specs written, not run here). noindex unchanged (IAM-509 owns SEO). Critics returned zero blocking findings, arbiter accepted on iteration 1; no remaining blockers (release-gate CI/browser items below are out of the authoring loop). |
| IAM-505 | Phase 5: built-in abuse-case sample policies (visible, fictional, deterministic) | accepted | 1 | 0 | New content/tools/iam-blast-radius/samples.js + app.js sample-loader controls (renderSamples() wired into init) render a small keyboard- and mouse-accessible set of built-in sample policies; loading a sample fills the input and runs the normal analyze flow (incl. the coverage panel). Samples are fictional (placeholder ARNs / account 111122223333) and deterministic. Set covers the required obvious-escalation case (escalation-passrole-lambda: iam:PassRole scoped to Lambda + lambda:CreateFunction = compound critical path) plus three scary-but-neutralized cases proving the tool does NOT overstate: neutralized-explicit-deny (broad object-read Allow fully suppressed by a conflicting explicit Deny -> no DATA-EXFIL), neutralized-permissions-boundary (broad service wildcards but no iam:PassRole / no IAM admin -> high, never critical, no escalation path), neutralized-unsupported-notprincipal (dangerous-looking NotPrincipal resource policy fails closed BEFORE rule evaluation -> blocking coverage state UNSUPPORTED_NOTPRINCIPAL with exact JSON path rather than confident findings). Each UI sample has a matching engine regression fixture under fixtures/samples/ reusing the frozen negative-corpus / family intent (a UI sample is not a substitute for a fixture). Safe DOM only (createElement+textContent; sample buttons via addEventListener, no inline style/script/on-handlers); styles.css adds sample-loader styling. New tests/samples.test.js runs every sample fixture through analyze() and asserts deterministic output matching its contract; e2e tests/e2e/samples.spec.js covers keyboard+mouse load and the neutralized fail-closed/Deny-suppression demonstrations (browser matrix is CI's job). Full node --test suite green (547 pass, 0 fail); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py clean on the shipped tree; QA + compat/a11y + security critics clean; critics returned zero blocking findings, arbiter accepted on iteration 1 (max 4). No remaining blockers. |
| IAM-506 | Phase 5: condition classification v1 (classify, do not claim runtime allow/deny) | accepted | 1 | 0 | New engine/conditions.js (pure, deterministic, deep-frozen) classifies each Condition entry by how the TEXT reads - constraint (narrows when/where/who), selector (selects service/resource/encryption-context), expansion (negated / wildcarded / missing-key broadening), or context-required (unmodelled key / unresolvable operator) - and NEVER claims a runtime AWS allow/deny (threat-model T8). Covers the required keys: aws:MultiFactorAuthPresent (MFA), aws:SourceIp/SourceVpc/SourceVpce, aws:PrincipalOrgID, aws:RequestedRegion (constraints); iam:PassedToService, iam:AssociatedResourceArn, kms:ViaService (selectors). parseOperator() strips ForAllValues:/ForAnyValue: set qualifiers + ...IfExists suffix; NEGATED operators (StringNotEquals/NotLike/ArnNot*/NotIpAddress/Numeric/DateNot*) on a constraint key => expansion (broadening), on a selector => select-by-exclusion; Null:true => missing-key broadening (expansion), Null:false => presence-only constraint (uncredited); Bool false on MFA => broadening; wildcard "*" value => expansion. `credited` (reads as a dependable guardrail) is true ONLY for a known constraint key with a plain narrowing operator; ...IfExists and ForAllValues (absent-key footguns) drop credit and are noted; selectors, expansions, and everything context-required are NEVER credited. WIRING: analyze.js stamps a `conditionClassification` onto every finding (additive explainable-evidence; does NOT change severity/exploitability - the rule/escalation engines already fold conditions into those numbers) and passes unsupportedConditionKeys(model) into enrichCoverage so unmodelled keys surface as coverage.summary.unsupportedConditions and mark coverage INCOMPLETE (unsupported does NOT mean safe; an unknown condition never lowers risk to safe - the broad finding still fires). aws:RequestedRegion added to the known catalog so the existing conditioned fixtures do not spuriously flip to incomplete; the only unmodelled key in the corpus (sts:ExternalId) sits in an already-blocked resource/trust fixture. report.js JSON carries conditionClassification verbatim; Markdown gains a per-finding "Condition classification (how the text reads, not a runtime verdict)" bullet list (mdSafe()'d so hostile keys/operators stay inert). app.js renders the classification in the per-finding detail (safe DOM: createElement+textContent, colour-coded cc-narrows/selects/broadens/context-required with a textual [tag] so meaning survives without colour); styles.css adds the list styling (no inline style/script). New fixtures/condition-class/ (unknown-condition-not-credited, known-keys-mixed-classes, footgun-operators-not-credited) + new tests/conditions.test.js (23 cases: parseOperator, per-key constraint/selector classification, negated/wildcard/Null/Bool/IfExists/ForAllValues footguns uncredited, unknown context-required, aggregation + determinism, fixture-driven per-entry contracts, and analyze()-level coverage-incomplete + finding-still-fires + evidence + JSON/MD carriage). Full node --test suite green (570 pass, 0 fail); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree. Updated tests/e2e/ui-shell.spec.js (finding detail shows the classification without a runtime verdict; unmodelled key surfaces in the coverage panel as unsupported/incomplete while the finding still fires) - browser matrix is CI's job. noindex unchanged (IAM-509 owns SEO). Interactive request-context simulation intentionally OUT of scope for v1. Critics returned zero blocking findings, arbiter accepted on iteration 1; no remaining blockers. |
| IAM-508 | Phase 5: CSP single source of truth (HTTP header authoritative) | accepted | 1 | 0 | Cloudflare HTTP CSP header treated as authoritative; index.html <meta> kept as a documented weaker fallback (header MINUS frame-ancestors, since a meta tag cannot enforce frame-ancestors). The exact authoritative header value is mirrored in three places kept in sync: docs/DEPLOY.md (the value Oliver pastes into Cloudflare), an index.html `AUTHORITATIVE-CSP-HEADER: <value>` comment, and the live index.html <meta> fallback. New tests/csp-consistency.test.js is the drift guard (4 cases): all three copies present/extractable, comment mirror equals DEPLOY.md header exactly, meta equals header minus frame-ancestors, and meta never carries frame-ancestors or adds/drops any other directive. No build step introduced (per Oliver's no-build decision); asset coherence continues to rely on the per-deploy Cloudflare cache purge. No engine/runtime change; csp_audit.py clean (no inline anything); full node --test suite green; security critic clean; critics returned zero blocking findings, arbiter accepted on iteration 1 (max 3). No remaining blockers (the Cloudflare route CSP-header add remains Oliver's release-gate item below). |
| IAM-507 | Phase 5: small versioned curated action catalog + unknown-action reporting | accepted | 1 | 0 | New engine/catalog.js (pure, deterministic, dependency-free, zero network) ships a SMALL, CURATED, DATED action snapshot (ACTION_CATALOG_VERSION 2026.08.22 / ACTION_CATALOG_DATE 2026-08-22) - action existence + AWS access-level annotation (List/Read/Write/Permissions management/Tagging) for the services the rules already reason about (iam, sts, s3, lambda, ec2, kms, secretsmanager, cloudtrail/guardduty/config, ecs, glue, codebuild, sagemaker, datapipeline, cloudformation, ssm, dynamodb, rds) - NOT the full AWS service-reference build. Exposed behind a CatalogProvider interface (defaultCatalog: version/date/hasService/hasAction/lookup) so a generated/sharded catalog can replace it later WITHOUT touching rules.js or escalation.js; lookupAction()/unrecognizedActions(model, catalog) take an optional provider (default = shipped snapshot) and a stub-provider test proves the swap. Case-insensitive matching; wildcards/patterns (*, s3:*, s3:Get*) are never exist-checked or reported as unknown - only fully-concrete tokens are. WIRING: analyze.js imports the catalog and passes unrecognizedActions(model) + the dated catalogVersion into enrichCoverage on BOTH the supported and blocked paths; the IAM-502 coverage plumbing (summary.unrecognizedActions, versions.catalogVersion, the app.js coverage panel + report.js JSON/MD "Action-catalog version" / "Unrecognized actions" lines) was already in place, so unknown actions now SURFACE in coverage with the version and mark coverage INCOMPLETE (unsupported != safe; the unknown action never suppresses a finding). Top-level result.catalogVersion stays '1' (the RULE/finding catalog, "Rule catalog version" in exports) - distinct from the action snapshot, which versions on its own cadence. The curated set recognizes every concrete action across the existing fixtures/tests/samples so no prior fixture spuriously flips to incomplete. New tests/catalog.test.js (16 cases: provider interface + case-insensitive lookup, known/unknown-action-known-service/unknown-service/wildcard/malformed resolution, unrecognizedActions sort/dedup/NotAction-scan/null-tolerance/determinism, interface swappability via a stub, and analyze()-level unknown-action surfacing + dated version in coverage + JSON/MD exports + finding-still-fires). Full node --test suite green (586 pass, 0 fail; +16); gate:no-network + gate:no-unsafe-dom clean on the shipped tree; no HTML/CSS change (csp_audit unaffected). e2e coverage-version assertion (rule catalog text) unchanged; browser matrix is CI's job. noindex unchanged (IAM-509 owns SEO). |
| IAM-509 | Phase 5: STAGE the SEO/indexing launch bundle (do NOT remove noindex) | accepted | 1 | 0 | STAGED the full indexability bundle in the shipped index.html <head> while KEEPING the gate closed: `<meta name="robots" content="noindex">` is intentionally retained and NO sitemap entry is added (this static tool is not emitted by Pelican's sitemap plugin, and none was hand-added), with a `TODO(Oliver): flip noindex + add sitemap + submit to Search Console when the Phase-B gates are signed off` comment marking the human flip. Added: self-referential `<link rel="canonical" href="https://rivassec.com/tools/iam-blast-radius/">`, an accurate `<meta name="description">` (uses "potential blast radius", never "policy simulator"/"effective permissions"), Open Graph (og:type/url/site_name/title/description/image), Twitter Card (name="twitter:card" summary_large_image + title/description/image pointing at the existing self-hosted /images/og-default.png), and a truthful SoftwareApplication JSON-LD data block (application/ld+json - not executable, CSP-safe under script-src 'self'; @type SoftwareApplication, name/url=canonical/description/offers price 0/isAccessibleForFree/author). Refined <title> to "client-side AWS IAM policy analyzer". Added a crawlable, visible "About this tool" docs <section> (styles.css .docs, no inline style) covering supported policy families (identity fully evaluated; resource/trust/boundary/SCP-RCP/session detected + fail-closed; NotPrincipal rejected), coverage behavior ("clean parse != complete conclusion", "unsupported does not mean safe"), limitations (single-document, no AWS calls/credentials, conditions classified not simulated, small dated action catalog), privacy boundary (no network calls in shipped JS + CSP defense-in-depth, no storage, Clear/pagehide wipe - stated WITHOUT absolute claims), examples (built-in samples), rule catalog (capability + escalation families, split certainty, independently-versioned rule/action catalogs), and how it differs from AWS IAM Access Analyzer / Policy Simulator (those reason against live account context; this is a static single-document client-side analyzer, complementary not a replacement). Every structured-data claim is mirrored in the visible docs (JSON-LD description appears verbatim as the opening docs paragraph; free/no-credentials/browser claims made visible). SEO wording rules honored: "potential blast radius" used, no self-claim of "policy simulator"/"effective permissions" (AWS's Policy Simulator referenced only as a distinct product in comparison prose), NO meta keywords, NO absolute claims ("100% private"/"mathematically prove"/"zero exfiltration" absent), no rich-result promise. New tests/seo-metadata.test.js (15 cases: noindex retained, TODO gate comment, no hand-added sitemap.xml, canonical correctness, description+OG+Twitter presence, og/twitter image exists on disk, JSON-LD valid+truthful+url==canonical, structured-data-visible-on-page, self-description banned-word guard, no keywords, no absolute claims, no rich-result promise, docs section covers every required topic). Full node --test suite green (605 pass, 0 fail; +15); gate:no-network + gate:no-unsafe-dom + scripts/csp_audit.py all clean on the shipped tree (JSON-LD block is a non-executable data block, not flagged). e2e specs unchanged (docs section is additive; app.js/DOM ids untouched); browser matrix is CI's job. No blocking findings left in the authoring loop; remaining blockers are Oliver's human release-gate flip (retire noindex + add sitemap entry + submit to Search Console once the Phase-B gates below are signed off). |
| IAM-601 | Phase 6 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-602 | Phase 6 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-603 | Phase 6 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-701 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-702 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-703 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-704 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-705 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-706 | Phase 7 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-604 | Phase 6: mutation testing (nightly) + version-coherence self-test | accepted | 1 | 0 | Mutation testing: `tools/iam-blast-radius/stryker.conf.js` (ESM config, `@stryker-mutator/core` 8.7.1 pinned exact in package.json devDeps, `npm run mutation` script) targets the deterministic decision engine (evaluator/rules/escalation/correlate) via the command test runner reusing the exact `node --test "tests/**/*.test.js"` command; thresholds break=90 (>=90% score target). Wired as NIGHTLY, non-PR-blocking `.github/workflows/iam-blast-radius-mutation.yml` (schedule cron 17 7 * * * + workflow_dispatch; actions/checkout + actions/setup-node pinned to the same commit SHAs the CI workflow uses); NOT run in this loop (nightly CI runs it). Version-coherence: new shipped `engine/version.js` declares the canonical VERSION_MANIFEST (ruleVersion '1', actionCatalogVersion '2026.08.22', buildSha 'dev') + pure diffVersions()/checkVersionCoherence() comparing the live shipped identifiers (analyze.CATALOG_VERSION, coverage.RULE_VERSION, catalog.ACTION_CATALOG_VERSION, coverage.BUILD_SHA) to the manifest (version.js imports those constants; they do not import back -> no cycle). worker.js fallback catalogVersion now derives from CATALOG_VERSION (no more drift-prone literal). app.js runs checkVersionCoherence() at startup and FAILS CLOSED on mismatch (disables Analyze + file input, renders a safe-DOM alert, and analyzeText() guards on the block) - a torn/partial deploy never analyzes against inconsistent modules (threat-model T8). New `tests/version-coherence.test.js` (8 cases): manifest frozen/shape, every shipped identifier matches, checkVersionCoherence() ok, drift + missing-identifier detection, altered-manifest not-ok block path, every corpus finding carries the manifest ruleVersion, worker.js derives (not hardcodes) its fallback. Full node --test green (627, +8); gate:no-network + gate:no-unsafe-dom + csp_audit clean; .gitignore adds reports/ + .stryker-tmp/. |
| IAM-707 | Phase 7: acceptance-suite fixturization (all 24 + 22A/B/C) + 12 cross-test invariants harness + full re-test | accepted | 1 | 0 | New `tests/acceptance-suite.test.js` is the single gating harness tying `docs/acceptance-suite.md` to committed fixtures under `fixtures/acceptance/`, driven from real `analyze()` output. COMPLETENESS GATE: all 24 tests (with 22 expanded to subcases 22A/22B/22C) = 26 ids, each with exactly one fixture; a missing/duplicate/orphan fixture FAILS (zero silent skips). Authored the 11 missing acceptance fixtures (6, 9, 10, 15, 16, 17, 18, 20, 22A, 22B, 23) alongside the 15 landed by IAM-701..706. PER-FIXTURE: expect (valid/findingIds/notFindingIds) + coverageExpect (blocked/codes/path/preservedVersion) + errorExpect (validation codes) + severityExpect (severity + split confidence + resources) + graphExpect (required/forbidden edges). DESIGN-BLOCKED: role-trust family tests 10/15/16/17/18 fail closed by design (UNSUPPORTED_POLICY_FAMILY); each fixture carries a `designBlocked` marker naming the queued follow-up (role-trust family analysis, next feature per Oliver 2026-08-22) + the intended finding, and the harness asserts the CURRENT blocked state so a later story flips it deliberately. The 12 CROSS-TEST INVARIANTS encoded as reusable helpers and applied across every applicable fixture: family-explicit, evidence-immutable (every attributed action granted by its exact statement; every referenced index exists), AND/OR-explicit (escalation prerequisites anyOf/allOf), unknown-visible (non-empty limitation on every finding), denies-are-not-grants (no capability edge cites a Deny statement), condition-polarity (classifier wired on every finding), no-semantic-edge-reuse (no edge aggregates unlike capability classes), dedup-explainable (subsumed keep id+prose; risk factors labeled), exports-agree (JSON+Markdown same finding set/severities; counts agree), invalid-fails-closed (ok:false -> no findings/graph/model; blocked -> zero findings + no capability edges), local-only (static no-network scan over shipped engine sources), determinism (byte-identical export on re-analysis). SCOREBOARD written to `docs/acceptance-suite-results.md`: 6 originally-passing preserved (6,11,20,22A,22B,23), 15 FIXED this phase (1,2,3,4,5,7,8,9,12,13,14,19,21,22C,24), 5 DESIGN-BLOCKED (10,15,16,17,18). Engine untouched (test + fixture data only); one non-weakening guard added to `acceptance-provenance.test.js` (skip the vacuous provenance check on ok:false 22A/22B, matching the compound harness pattern). Full node --test green (763 pass, 0 fail, 0 skipped, +68); gate:no-network + gate:no-unsafe-dom clean; originally-passing tests + negative corpus not regressed; Playwright matrix remains CI's job. |
| IAM-800 | Phase 8 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-801 | Phase 8 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-802 | Phase 8: public/anonymous + cross-account trust findings + external-origin graph | human-review | 1 | 1 | BLOCKER (unresolved): [test/critical] trust.js:525: e -> r. Original notes: Trust findings (TRUST-PUBLIC critical, TRUST-CROSS-ACCOUNT high/low-with-ExternalId) were already emitted by engine/trust.js (IAM-801). This story adds the missing external-origin GRAPH: new buildTrustGraph(model, findings) in engine/graph.js builds ExternalPrincipal/Service origin node(s) -> a typed `can-assume` edge -> a single `role:trust-target` Role node marked `unknownPrivileges` (target-role privileges out of scope, T8). Origin represents EXTERNAL principals, never the identity "Principal (subject of this policy)" root (acceptance test 15); no identity capability/broad-Resource edges on a trust policy; reuses the typed can-assume edge, no generic can-write aggregation. Edge certainty: unconditioned trust = confirmed-by-policy, conditioned = context-required (text-only, never a runtime STS allow/deny). Wired into analyze.js trustResult (was shipping emptyGraph). render-graph.js: computeLayout now roots a principal-less trust graph at its in-degree-0 external origin(s) and draws NO phantom principal-subject node (identity graphs + the empty-graph default unchanged, byte-identical). Node/edge cap (T5) + deterministic sort + deepFreeze mirror buildGraph. Added graphExpect to acceptance fixtures test-15/test-16; 6 new tests (trust graph shape in trust.test.js, principal-less layout + inert render in render-graph.test.js). Full node --test green: 792 pass (was 786). No shipped-code network APIs / innerHTML / eval added. |
| IAM-804 | Phase 8 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-805 | Phase 8 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-803 | Phase 8: trust condition polarity (org-id expansion, confused-deputy constraints) | human-review | 1 | 1 | BLOCKER (unresolved, read-only investigation - no source edited): [correctness/high] engine/trust.js:784 severity assignment in branch 4 TRUST-CROSS-ACCOUNT (driven by hasConfusedDeputyConstraint at trust.js:458-460, branch at :735) floors severity to LOW whenever a confused-deputy constraint (sts:ExternalId, aws:SourceArn/SourceAccount, or StringEquals aws:PrincipalOrgID) is present WITHOUT a principalIsBroad guard. Policy {"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"AWS":"arn:aws:iam::*:role/*"},"Action":"sts:AssumeRole","Condition":{"StringEquals":{"sts:ExternalId":"shared-not-secret"}}}]} yields [low] TRUST-CROSS-ACCOUNT (pathExpl=low). Same all-accounts wildcard principal with NO condition yields [high] (U1b); a single specific account + ExternalId also yields [low] - so a non-secret, non-auth ExternalId collapses the broadest non-anonymous principal (every role in every account) from high to low, equal to a properly-scoped single-vendor trust. Three proofs: (1) monotonicity/under-claim - two-band drop on the broadest principal via a value the tool's own text calls "NOT authentication and NOT a secret"; (2) prose/evidence contradiction - why text says "the trusted account still delegates to any identity ITS administrator authorizes" (singular) while evidence.principals records "arn:aws:iam::*:role/*" (all accounts); (3) internal inconsistency - principalIsBroad()/principalValueIsBroad() return TRUE for this value and gate the org-exclusion critical path at trust.js:619 and the no-condition high case, but branch 4's `if (constrained) severity='low'` has no such guard, re-introducing the single-scope-evidence-vs-broad-reality defect iteration-4 fixed on the org-exclusion path. aws:SourceArn/SourceAccount reproduces identically (low); the StringEquals aws:PrincipalOrgID variant is a weaker related case (bounds to org members) and NOT marked blocking. FIX DIRECTION (not applied): in branch 4, when hasConfusedDeputyConstraint is true but principalIsBroad(principals) is true, do NOT floor severity at low - an all-accounts/org-wide wildcard principal has no identified third party for an ExternalId to correlate, so keep severity high (constraint lowers path-exploitability only), mirroring trust.js:619 and the IAM-802-B defense-in-depth treatment; also stop the why text saying "the trusted account" (singular) for an all-accounts principal. ALL OTHER SCENARIOS PASS (specific-account+ExternalId->low; account no-cond->high; Principal "*"->critical public; "*"+StringNotEquals PrincipalOrgID->critical org-expansion; OIDC repo:org/* broad sub->high; OIDC tight sub no aud->low; service lambda->info; "*"+StringEquals PrincipalOrgID->high; account-pinned wildcard ARN+ExternalId->low). Repro via scratchpad harnesses probe.mjs/probe2.mjs/cmp.mjs (each imports engine/analyze.js). PRIOR NOTES (IAM-803 landed content): The trust FINDINGS + severities (TRUST-ORG-EXPANSION critical on StringNotEquals aws:PrincipalOrgID with a broad principal; ExternalId/SourceArn/SourceAccount lowering cross-account trust; MFA/SourceIp as defense-in-depth) were already correct in engine/trust.js from IAM-801/802. This story closes the classifier/coverage INCOHERENCE: the Phase-5 condition classifier (engine/conditions.js) did not model the trust condition keys, so a trust policy's understood conditions (sts:ExternalId, aws:SourceArn/SourceAccount, aws:PrincipalArn/PrincipalAccount, OIDC/SAML aud+sub) were wrongly reported as `unsupportedConditions` and flipped coverage to incomplete, and each finding's conditionClassification called ExternalId "not modelled" while the trust evaluator credited it as a confused-deputy constraint. Extended KNOWN_KEYS with the five static trust keys (constraints; a NEGATED operator flips to expansion via the existing branch, so StringNotEquals aws:PrincipalOrgID reads as expansion/exclusion - never "missing"); added a federationMeta() resolver for the provider-host-prefixed OIDC/SAML `...:aud` (constraint) and `...:sub` (constraint, but a wildcarded `repo:org/*` subject is broad and NOT credited as a dependable guardrail). Classification stays text-only (no runtime STS allow/deny claim). Genuinely unknown trust keys (e.g. sts:RoleSessionName) still stay context-required, reduce confidence, and surface in coverage (unsupported != safe). Coverage on tests 10/16/17 now complete + conditionClassification coherent with the trust severity. No shipped HTML/DOM/network changes (conditions.js clean of fetch/innerHTML/eval; no new output fields so report.js/app.js render unaffected). +11 tests (6 conditions.test.js unit + 3 trust.test.js integration + 1 condition-class fixture trust-condition-keys-modeled.json): full node --test green 820 pass (was 809), deterministic. Identity-family behavior and the Phase-7/negative corpora unchanged (no non-trust fixture uses these keys). |
| IAM-806 | Phase 8: flip role-trust acceptance fixtures + trust cross-invariants + full re-test | accepted | 1 | 0 | Phase-8 capstone. Confirmed the five role-trust acceptance fixtures (tests 10/15/16/17/18) are flipped from their `designBlocked` fail-closed markers (Phase-7 `UNSUPPORTED_POLICY_FAMILY`) to their REAL expected trust findings + severities, driven from real analyze() output by tests/acceptance-suite.test.js (DESIGN_BLOCKED_IDS is now empty; the mechanism is retained for a future unsupported family). Verified against docs/trust-policy-semantics.md + docs/acceptance-suite.md prose: test 10 TRUST-ORG-EXPANSION critical (StringNotEquals aws:PrincipalOrgID classified expansion/exclusion, never "missing"), test 15 TRUST-PUBLIC critical (no identity broad-Resource finding on an omitted Resource), test 16 TRUST-CROSS-ACCOUNT low (sts:ExternalId a confused-deputy constraint, never "missing"/auth/secret), test 17 TRUST-FEDERATED high (aud a valid constraint, repo:example-org/* sub broad-uncredited, recommends repo+ref/env), test 18 TRUST-SERVICE informational negative control. Every trust finding carries the target-role-permissions-out-of-scope limitation. Applied the 12 cross-test invariants across the trust fixtures via the existing acceptance-suite harness (evidence-immutable, condition-polarity, unknown-visible, denies-are-not-grants, no-semantic-edge-reuse, exports-agree JSON/MD, determinism) - all pass. STRENGTHENED the trust fixtures: added graphExpect to tests 10/17/18 (mirroring 15/16) asserting the external-origin `can-assume` edge (ext:anonymous / ext:...:oidc-provider/... / svc:lambda.amazonaws.com -> role:trust-target) and excluding any identity `principal`-subject origin + can-write/can-read aggregation. NotPrincipal stays fail-closed UNSUPPORTED_NOTPRINCIPAL (not flipped; asserted in family.test.js + trust.test.js). Updated docs/acceptance-suite-results.md: tests 10/15/16/17/18 now FIXED (Phase 8), tally + design-blocked section + verification refreshed. DATA + docs only - no shipped engine/HTML/CSS/JS change (gate:no-network + gate:no-unsafe-dom exit 0 on the shipped tree; csp_audit is CI's built-output gate). Full node --test green 880 pass, 0 fail, 0 skipped (was 880 - fixture strengthening added assertions, not tests). Phase-7 fixes, the 6 originally-passing tests, and the identity + trust negative corpora not regressed. NOTE for the human reviewer: IAM-802 and IAM-803 remain in human-review with recorded blockers (IAM-803: engine/trust.js branch-4 floors a broad all-accounts wildcard-ARN principal + ExternalId to LOW where docs/trust-policy-semantics.md section 5 says breadth stays HIGH - a real severity-underclaim defect). That case is NOT one of the acceptance-suite tests 10/15/16/17/18 and was left to IAM-803's owner; IAM-806 did not touch trust.js severity logic. |
| IAM-901 | Phase 9 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-902 | Phase 9 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-903 | Phase 9 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-904 | Phase 9: re-run both acceptance suites + scoreboard | accepted | 1 | 0 | Drove every suite-2 policy block (tests 25-54) directly through analyze()/validate(). In-scope Phase-9 fixes confirmed green: test 44 -> DUPLICATE_JSON_KEY blocked; test 34 -> ROLE-TAKEOVER critical (per-statement evidence, no PassRole); test 48 -> TRUST-INVALID-PRINCIPAL + incomplete coverage. Supported identity/trust tests (25/35-42/45/46/47/50/54) all pass with no overstatement. Deferred families stay honestly handled and are NOT silently skipped: resource/RCP fail closed (UNSUPPORTED_POLICY_FAMILY), SCP (UNSUPPORTED_SCP_SHAPE), NotPrincipal (UNSUPPORTED_NOTPRINCIPAL); permissions-boundary(30)/session(31) analyzed-as-identity family-gap. Each of the 13 deferred tests encoded as a designBlocked fixture under fixtures/acceptance-2-deferred/ + driven by tests/acceptance-suite-2-deferred.test.js (14 assertions) so a future family tranche must flip them deliberately. Scoreboard written to docs/acceptance-suite-2-results.md. Regression: suite 1 still 24/24, identity + trust negative corpora unchanged and green. No engine code changed in this story (901/902/903 already landed). |

| IAM-1001 | Phase 10 (P0): mandatory policy-family selection contract | accepted | 1 | 0 | Policy-family selection is now MANDATORY (docs/acceptance-suite-3.md Campaign B, tests 64/67/68/69/70/71). ENGINE: detectFamily() gains `requireExplicitFamily` + treats `family:'auto'`/`'auto-detect'` as an EXPLICIT opt-in choice; with requireExplicitFamily and no selection it fails closed BEFORE shape classification with new code POLICY_FAMILY_REQUIRED (detected='unknown', family=null - never a shape-based identity default, test 64). New family-shape guards under an explicit selection: Identity + a Principal -> UNSUPPORTED_PRINCIPAL at Statement[i].Principal (never drops the Principal, test 67); Role-trust + a Resource on a Principal-bearing doc -> UNSUPPORTED_TRUST_RESOURCE at Statement[i].Resource (test 68); Resource/SCP-RCP selected -> UNSUPPORTED_POLICY_FAMILY naming the family, input preserved (test 69). analyze()/worker.js/app.js thread the option through; ENGINE stays back-compatible (callers omitting requireExplicitFamily still auto-detect, so the existing 956 unit tests + suite-1/suite-2 fixtures are untouched). UI: index.html select defaults to a "Select a policy family..." placeholder (value ''), adds an explicit Auto-detect option, and Analyze starts `disabled`; app.js enables Analyze only when a family is chosen, and a family CHANGE invalidates prior findings/coverage/graph/evidence/export immediately and auto-reanalyzes under the new family (test 70) - no identity result left under a boundary label. EXPORTS: report.js adds analysisStatus() (ok/warned/blocked/error) + selectedFamily + warnings to JSON and Markdown; the browser publishes the same status via #status[data-status] so all three surfaces agree, and a blocked export is never labeled authoritative (test 71). New fixtures/family-selection/ (64/67/68/69/69b + two positives) + tests/phase10-family-selection.test.js (18 assertions incl. status parity across JSON/MD/browser). Updated family.test.js (identity-override-on-Principal now UNSUPPORTED_PRINCIPAL) + Playwright specs (mandatory-selection + tests 64/67/68/69/70/71; all analyze flows opt into Auto-detect). Full node --test green 974 pass, 0 fail; csp_audit clean; no-network + no-unsafe-dom gates clean. permissions-boundary/session envelope semantics (tests 65/66) are IAM-1002; full resource-policy/RCP remain the honest fail-closed later tranche. |
| IAM-1002 | Phase 10 (P0): permissions-boundary + session-policy envelope semantics | accepted | 1 | 0 | Permissions-boundary + session-policy envelopes now analyzed as intersecting envelopes rather than identity policies (docs/acceptance-suite-3.md tests 65/66). Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-1003 | Phase 10 (P1): role-takeover correlation hardening (overlap + condition compatibility) | accepted | 1 | 0 | docs/acceptance-suite-3.md Campaign C (tests 72-80). The overlap + condition-compatibility engine logic already landed in the IAM-1001/1002 iterations (escalation.js detectRoleTakeover: legs retain per-statement resources so a wildcard modify grant is matched against a concrete assumable role via resourceCoversRole() overlap - test 74 anchors the takeover on the concrete intersecting role deployment/Prod, never generalized to deployment/*; principalConditionsSatisfiable() is EXISTENTIAL across one statement per leg-group so simultaneously-unsatisfiable same-key aws:PrincipalAccount pins on the modify vs assume legs do NOT correlate - test 75 - while the standalone PUT-INLINE-POLICY / TRUST-POLICY-MODIFY capabilities remain and both condition expressions are preserved in model.statements). This story adds the missing DELIVERABLE: fixtures/acceptance-3/ with the required test-74 (positive overlap) and test-75 (negative contradictory-conditions) plus the enumerated Campaign-C regression set (72 exact same-role, 73 different-roles, 76 Deny-removes-prerequisite, 77 AttachRolePolicy alternative, 78 modify-without-assume, 80 duplicate-modify-no-duplicate-edges), each driven through real analyze()/validate() by the new tests/acceptance-suite-3-fixtures.test.js harness (generic valid/status/findingIds assertions plus a ROLE-TAKEOVER-specific expect.takeover block: count, severity, anchored resources, subsumed primitives, per-statement evidence provenance for test 79, why-includes/excludes, and a graphNoDuplicateEdges invariant for test 80). Verified all regression cases behave correctly before locking them (72 one-critical, 73 no-correlation, 76 residual PutRolePolicy w/ trust-modify denied, 77 critical via ATTACH-POLICY w/ iam:PolicyARN evidence, 78 high role-control no self-assumption, 80 one path + one can-modify edge). DATA + test only - no shipped engine/HTML/CSS/JS change. Full node --test green 1023 pass, 0 fail, 0 skipped (was 1007: +8 harness + 8 determinism-corpus checks on the new fixtures). No regression to suite 1, suite 2, the identity negative corpus, or the trust negative corpus. |
| IAM-1004 | Phase 10 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-1006 | Phase 10 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-1007 | Phase 10 | accepted | 1 | 0 | Accepted on iteration 1; critics returned zero blocking findings, arbiter accepted. No remaining blockers in the authoring loop. |
| IAM-1008 | Phase 10: fixturize all three suites + re-run all three + scoreboard | accepted | 1 | 0 | Phase-10 capstone (DATA + tests + docs only; no shipped engine/HTML/CSS/JS change). Made suite-3 completeness enforceable and wrote the scoreboard. NEW single authoritative gate `tests/acceptance-suite-3-results.test.js`: a MANIFEST enumerates every suite-3 test 55-100 (docs/acceptance-suite-3.md, campaigns A-F), asserts each is accounted for EXACTLY ONCE (zero silent skips), and for the 40 fixture-backed cases DERIVES the result state from real analyze()/validate() output and asserts it equals the declared verdict (so the scoreboard can never drift from engine behavior) + a per-case determinism check; the 6 procedure/UI-only cases (63 paste/import parity, 70 family-switch invalidation, 71 export-surface parity, 79 cross-statement provenance, 98 state isolation, 100 limit sweeps) each name their covering unit/Playwright file, asserted to exist. FIXTURIZED the campaigns that lacked committed fixtures: 13 new fixtures under fixtures/acceptance-3/ - Campaign A parser hardening (55/56/57/58/59 dup-key BLOCKED with exact code+path, 60 DUPLICATE_SID warning, 61 INVALID_JSON, 62 BOM-stripped) via `policyRaw`, and Campaign F false-positive control (92/93/94 required-wildcard actions -> no remediable WILDCARD-RESOURCE, 96 ForAllValues+Null suppresses the test-41 vacuous-truth warning, 97 empty ForAnyValue -> no phantom capability); tagged the existing test-99 injection fixture with acceptanceTest. Extended tests/acceptance-suite-3-fixtures.test.js with `errorExpect` (code/path/message) handling so the Campaign-A fixtures' DUPLICATE_JSON_KEY/INVALID_JSON codes are enforced by the deep harness. Campaigns B (64-69 in family-selection/ + family-envelope/), C (72-80), D (81-85), E (86-91) reuse their existing committed fixtures. Re-ran all three suites: suite 1 = 24/24 (acceptance-suite.test.js completeness gate); suite 2 targeted fixes intact (34 role-takeover, 44 duplicate-key, 48 invalid-principal, plus 30/31/36/38/41/50) via acceptance-suite-2.test.js; suite 3 campaigns A-F pass per the release gate. The ONLY remaining honest fail-closed set is the resource-policy family + RCP - suite-2 26/27/28/32/33/49/51/52/53 stay BLOCKED with designBlocked markers (fixtures/acceptance-2-deferred/), and suite-3 test 85 (resource-policy Principal*+PrincipalArn) fails closed UNSUPPORTED_POLICY_FAMILY - documented as the next tranche. Wrote docs/acceptance-suite-3-results.md (per-test verdict 55-100 + release-gate checklist + deferred section). Full node --test green 1208 pass, 0 fail, 0 skipped (was 1134; +74); gate:no-network + gate:no-unsafe-dom clean on the shipped tree. No regression to suite 1, suite 2, the identity negative corpus, or the trust negative corpus. Playwright browser matrix remains CI's job. |
| IAM-1102 | Phase 11 (P0): required-critic panel id validation (fail closed on malformed critic id) | accepted | 2 | 0 | Accepted on iteration 2. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass). Per-critic outcome of the last (accepting) round: adversarial-review -> PASS; correctness/reliability -> PASS; security -> PASS; arbiter accepted on the all-PASS panel. Closes the IAM-1101 blocker: decide()/runReview (ralph/review-decision.mjs) now validate every required critic id is a non-empty string and FAIL CLOSED (configurationError INVALID_CRITIC_ID, accepted=false) on a number/object/empty-string id, exactly like the existing EMPTY_CRITIC_PANEL / DUPLICATE_CRITIC_IDS guards, so a malformed panel can never yield accepted=true nor commit a schema-invalid ledger row. |
| IAM-1103 | Phase 11C: invalid-family fail-closed + record regression fixtures | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass). Per-critic outcome of the last (accepting) round: adversarial-review -> PASS; correctness/reliability -> PASS; security -> PASS; arbiter accepted on the all-PASS panel. Delivered the durable regression gate for the record-test bundle: tests/record-cases.test.js drives the 9 engine-drivable BND-*/DEF-* cases (BND-01 + DEF-01..08) through analyze() under the adapter name-map and asserts blocked shapes emit zero findings + empty graph, required/forbidden rule ids, required prose, capability-edge budget, plus two meta-tests proving no BND-*/DEF-* case is silently skipped; tests/e2e/record-cases.spec.js adds named [CASE-ID] Playwright assertions for the 9 UI-only cases (green in chromium 9/9; firefox/webkit via CI matrix). Engine already fails closed on unrecognized/aliased family (family.js INVALID_FAMILY + FAMILY_ALIASES), closing the DEF-05 fail-open. Full node --test 1279 pass / 0 fail (+12); gate:no-network + gate:no-unsafe-dom + csp_audit clean; no shipped HTML/JS/CSS changed; no regression to suites 1/2/3 or the identity/trust negative corpora. |
| IAM-1101 | Phase 11 (P0): subject-account-aware PassRole viability | human-review | 4 | 1 | HELD ON A BLOCKING FINDING (not a critic non-pass): the adversarial critic RAN to completion and returned a verdict of BLOCKER with exactly 1 blocking finding in the last round - this is NOT a hold caused by a critic ERROR/TIMEOUT/INVALID_RESPONSE (no critic errored, timed out, or returned a malformed/null result; all critics produced valid results and the panel's outcome was PASS on two surfaces + BLOCKER on one). Per-critic outcome of the last round: adversarial-review critic across all three surfaces (11A/11B/11C) -> 11B (cross-account PassRole with ambiguous subject) SOLID/PASS; 11C (family-value fail-closed) SOLID/PASS; 11A (missing/null/errored/timed-out/malformed critic RESULTS - the primary surface the fix targets) ROBUST across ~24 hostile combinations (none reached accepted=true; every one wrote a durable ledger row except the two justified no-row classes: a true ledger-write failure, and the duplicate-id panel which cannot produce a schema-valid row) - PASS on the targeted surface, but the critic surfaced ONE fail-open in an adjacent under-guarded input = the 1 blocking finding => overall BLOCKER, story held. REASON FOR HOLD (the 1 blocking, medium): required-critic PANEL is under-guarded - decide()/runReview (ralph/review-decision.mjs:220 decide + :383-413 runReview panel guards) fail closed on an empty panel (EMPTY_CRITIC_PANEL) and a duplicate-id panel (DUPLICATE_CRITIC_IDS) but do NOT validate that each critic id is a non-empty string, so a panel with a number/object/empty-string critic id plus all-PASS responses returns accepted=true, decision='approved', and commits a SCHEMA-INVALID ledger row (review-ledger-entry requiredCriticIds.items {type:string,minLength:1}; critic-result criticId {type:string,minLength:1}), breaking the module's own invariant that every acceptance is durably AND validly recorded. Repro: runReview({requiredCritics:[123,'security'], responses:[{status:'PASS'},{status:'PASS'}]}) => accepted=true, ledgerRows[0].requiredCriticIds=[123,'security'], criticAttempts[0].criticId=123 (same accept+invalid-row for [{},'security'] and ['','security']). Reachability is limited to a caller passing a malformed requiredCritics array (NOT the attacker-controlled critic-response surface), so real-world exploitability is LOW - hence medium, not high - but it is a genuine accepted-review-on-a-malformed-panel fail-open. FIX DIRECTION (not applied - held for human review): decide()/runReview must validate every required critic id is a non-empty string and fail closed (configurationError, accepted=false, e.g. INVALID_CRITIC_ID) exactly like the empty-panel and duplicate-id paths, so a malformed panel never yields accepted=true nor commits a schema-violating ledger row. |
| IAM-1200 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1201 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1202 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1203 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1204 | Phase 12: adversarial review (same-account / confused-deputy / S3-typing) | human-review | 1 | 1 | HELD ON A BLOCKING FINDING (last round = 1 blocking). VERDICT ON THE FIX ITSELF: SOUND - all 7 named suite cases pass through BOTH analyzeResource() direct and the real text -> analyze({family:'resource', requireExplicitFamily:true}) pipeline against on-disk fixtures: T26 RESOURCE-CONFUSED-DEPUTY medium/unbound (not public); T27 correct SourceArn+SourceAccount info/source-bound (negative-control praise, no exposure fire); T53 mismatched account medium/mismatched naming both accounts (warns, no praise), symmetry probes hold; T28 Principal '*'+TLS-only-Deny stays PUBLIC-ACCESS critical, transportOnlyDeny annotated (object-form + SecureTransport-Allow variants also critical); T51 KMS account-root same-account RESOURCE-SAME-ACCOUNT-GRANT only ("not the root user only" wording correct); T32 same-account IAM user RESOURCE-SAME-ACCOUNT-GRANT with resource-vs-identity caveat; T33 assumed-role session ARN kept verbatim, never collapsed to role ARN. Fail-closed on missing/null/empty/blank/non-ARN/truncated-ARN/unsupported-service (ok:false, 0 findings, RESOURCE_CONTEXT_REQUIRED / UNSUPPORTED_RESOURCE_SHAPE, no over-claim). No FP/FN/over/under-claim in the same-account / confused-deputy / S3-typing logic IAM-1204 introduced. Principal:['*'] array-form fails CLOSED (INVALID_PRINCIPAL); NotPrincipal Allow fails CLOSED (coverage.blocked=true) - neither a defect. BLOCKER (unresolved, high; outside the changed logic - IAM-1202-era principal router, none of the 7 suite cases affected): engine/resource.js:984 (externalTypes set) via the resourceFindings principal loop only handles anonymous / service / {aws-account, aws-root, aws-principal-arn}; any other classified principal type is SILENTLY DROPPED -> ok:true with ZERO findings. Verified: Principal {AWS:'arn:aws:iam::*:root'} (aws-principal-arn-wildcard) -> 0 findings; {AWS:'arn:aws:iam::123456789012:role/app-*'} -> 0; {Service:'ec2-*.amazonaws.com'} (service-wildcard) -> 0; {CanonicalUser:'<hash>'} -> 0. CanonicalUser is a VALID common S3 resource-policy principal, so a legit cross/same-account CanonicalUser grant yields no finding - the line-323 'fail closed toward surfacing' promise is broken (canonical-user never enters namedEntries). Trust family fails these same wildcard types CLOSED with a coverage-warning; resource family emits nothing. Partial mitigation only: pipeline coverage still carries the INCOMPLETE caveat and enumerates the principal type (not a clean bill of health). FIX DIRECTION (not applied): route unmodeled principal types (canonical-user, aws-principal-arn-wildcard, service-wildcard, federated-wildcard) to a fail-closed coverage-warning finding mirroring the trust family, so a CanonicalUser grant or over-broad/invalid wildcard-ARN principal always surfaces >=1 finding instead of reading as zero. Also found: one dead variable (minor). Repro via scratch harnesses under tools/iam-blast-radius/ (scratch-critic-1204-fresh.mjs + scratch-dbg-1204*.mjs), engine unedited. |
| IAM-1205 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1206 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1207 | Phase 12 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1208 | Phase 12: resource-family adversarial hardening | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1300 | Phase 13 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1301 | Phase 13 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1302 | Phase 13 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |
| IAM-1303 | Phase 13 | accepted | 1 | 0 | Accepted on iteration 1. LAST ROUND = ALL-PASS: every required critic RAN to completion and returned a valid result of PASS - NO critic returned ERROR, TIMEOUT, or INVALID_RESPONSE, and none was null/missing/malformed, so acceptance is a genuine all-PASS panel approval (not an implicit approval from a critic non-pass); arbiter accepted on the all-PASS panel. No remaining blockers in the authoring loop. |

## Release-gate items requiring CI/browser (not the authoring loop)
- [ ] Playwright Chromium/Firefox/WebKit matrix green
- [ ] Real-device smoke (Android Chrome, iOS Safari)
- [ ] Mutation score for evaluator >= 90%
- [ ] npm audit + OSV clean; SBOM generated; license allowlist
- [ ] Cloudflare route CSP header added (Oliver)
- [ ] csp_audit.py clean on built output

## Phase 8 close-out - human verification of IAM-802 / IAM-803 (2026-08-23)
Both stories auto-marked `human-review` (their own iteration budget expired before
the ledger flipped). Reviewed all ~16 adversarial/QA-trust blocking findings by
driving `engine/analyze.js` directly with each finding's exact repro policy. RESULT:
every finding now yields the CORRECT severity in the FINAL committed engine - they
were STALE (logged against intermediate trust.js states mid-Ralph and resolved by
later iterations). Full 24-case suite passes; `node --test` 894/0. Residual cosmetic
only: a redundant `aws:PrincipalAccount` condition still shows `credited` in the
evidence panel while severity stays correctly high (not a severity bug; optional polish).
Verdict: no code fix required; safe to deploy.

## Phase 10 close-out + IAM-1005 reconciliation (2026-08-24)
All 8 stories accepted; node --test 1208/0; Suite 1 24/24, Suite 2 no failures
(11 resource/SCP/RCP fail-closed by design), Suite 3 39/39 applicable.
CAVEAT: during the run several critic calls hit transient API errors (529
Overloaded / server error / StructuredOutput retry-cap). IAM-1005's ENTIRE
critic panel + its ledger agent 529'd, so IAM-1005 was auto-accepted because its
critics returned null (no blocking findings) rather than because they approved -
and its progress.md ledger row was never written. I independently verified
IAM-1005 against the engine: T86 AddUserToGroup -> GROUP-MEMBERSHIP:high (not
DIRECT-IAM-ADMIN) OK; T87 ECS both roles -> two can-pass edges, task/exec
distinct, critical OK; T90 RegisterTaskDefinition-without-RunTask -> high staging
(not critical) OK. RESIDUAL (minor, not blocking): T91 cross-account PassRole
still emits PASSROLE-EC2:critical with only a prose same-account caveat (over-
claims in the safe direction). Workflow-robustness follow-up: treat a
critic-agent error as NON-pass (retry / block), not as an implicit approval.

## Post-release corrections (2026-08-24) - MUST stay in the release record
1. T91 is a KNOWN CONSERVATIVE FALSE POSITIVE, not a residual quirk. Honest
   Suite-III tally: 38/39 engine cases pass + 1 known conservative FP (T91) + 7
   procedural/UI cases. Fix queued Phase 11: subject-account-aware PassRole
   viability (differs-from-role-account -> suppress/ineffective; unknown-subject
   -> UNKNOWN viability, never critical).
2. CRITIC ERRORS MUST FAIL CLOSED (new workflow standard, applies to all future
   Ralph phases). Model each critic outcome explicitly as one of:
   PASS | BLOCKER | ERROR | TIMEOUT | INVALID_RESPONSE. ONLY all-PASS permits
   acceptance. Null / missing / malformed / errored / timed-out critic results
   must: (a) prevent auto-acceptance, (b) write a ledger entry, (c) record
   attempts + failure reason, (d) retry within a bounded policy, (e) require a
   successful review before promotion. Root cause of the IAM-1005 miss: a 529
   storm made all critics return null, which the harness read as an empty
   blocker list = implicit approval. That control failure is more serious than
   T91 because it compromises the review itself.

## IAM-1103 (Phase 11C) - invalid-family fail-closed + record regression fixtures (2026-08-24)
DELIVERED. Engine already fails closed on an unrecognized/aliased family
(family.js: INVALID_FAMILY + FAMILY_ALIASES scp/rcp/trust; trim+canonicalize
before matching), so the DEF-05 fail-OPEN (an 'scp'/'rcp' selection silently
analyzed as identity) is closed - a recognized control-family synonym
canonicalizes to scp-rcp and blocks UNSUPPORTED_POLICY_FAMILY, a garbage token
blocks INVALID_FAMILY, an empty selection stays auto-detect. This story adds the
durable regression gate for the record-test bundle:
- tests/record-cases.test.js (node --test): drives the 9 engine-drivable BND-*/
  DEF-* analyzer cases (BND-01 + DEF-01..08) through analyze() under the adapter
  name-map (BOUNDARY-ENVELOPE->PERMISSIONS-BOUNDARY-ENVELOPE;
  UNSUPPORTED_OR_INVALID_NOTPRINCIPAL->UNSUPPORTED_NOTPRINCIPAL;
  MIXED_OR_INVALID_POLICY_FAMILY->AMBIGUOUS_POLICY_SHAPE). Asserts blocked shapes
  emit zero findings + empty graph, required/forbidden rule ids, required prose,
  and capability-edge budget. Two meta-tests prove no BND-*/DEF-* case is silently
  skipped (each is either engine-driven or carries a named e2e marker).
- tests/e2e/record-cases.spec.js (Playwright, CI): named [CASE-ID] assertions for
  the 9 UI-only cases (BND-02..08 family-switch/browser-matrix/export/stale-state,
  DEF-09 unsupported->supported switch, DEF-10 blocked-export contract). Verified
  green in chromium (9/9); firefox/webkit run in CI via the project matrix.
Gates: full node --test 1279 pass / 0 fail (was 1267; +12); gate:no-network,
gate:no-unsafe-dom, csp_audit all clean; no shipped HTML/JS/CSS changed. No
regression to suites 1/2/3 or the identity/trust negative corpora.

## Phase 11 close-out + IAM-1101 human verification (2026-08-24)
Built: IAM-1102 (T91) accepted, IAM-1103 (invalid-family) accepted, IAM-1101
(critic fail-closed) held on 1 blocking. NOTE: the ledger-agent titles for
IAM-1101/1102 were mislabeled (title strings swapped) - the actual story->work
mapping per prd.json is 1101=critic fail-closed decision model, 1102=T91
subject-account PassRole, 1103=invalid-family fail-closed. The re-test confirms
all three deliverables green: node --test 1279/0; 15/15 workflow fault-injection
cases; 9/9 T91 cases; invalid-family fails closed; suites 1/2/3 + negative corpora
no regression.
IAM-1101 HOLD RESOLVED after independent verification of its 2 findings:
  1) A scratch debug file (engine/scratch-iam1101-critic.mjs) had leaked into the
     SHIPPED tree and would have deployed. REMOVED. Added tests/shipped-tree-
     hygiene.test.js (a CI gate that fails on any scratch/debug/test/tmp/.mjs stray
     under content/tools/iam-blast-radius/) so this + the Phase-9 leak class can
     never reach a deploy again.
  2) review-decision.mjs fail-OPEN on a malformed critic id: a later impl iteration
     already added the F4 INVALID_CRITIC_ID guard (decide() :246). Verified via the
     exact adversarial repro - runReview({requiredCritics:[123,'security'],...}) and
     ['',...]/[{},...] all now return accepted=false / review_error / INVALID_CRITIC_ID,
     while a valid all-PASS panel still accepts. Fail-open closed.
Both were the Phase-8 stale-ledger pattern (fixed by a later iteration / a one-line
cleanup) - IAM-1101 is effectively complete. The FAIL-CLOSED ARBITER worked: it held
1101 on a real blocker instead of auto-accepting, which is the whole point of 11A.

## IAM-1207 (Phase 12) - resource negative corpus + fixturize + re-run all three suites (2026-08-24)
DELIVERED. Two artifacts, no shipped-code change (the Phase-12 resource evaluator
already shipped in IAM-1201..1206).

1. Resource NEGATIVE corpus (the credibility artifact, like the identity + trust
   corpora): fixtures/negative-resource/ - 5 frozen "does-not-fire / low" contracts
   + rationale, each with the EXPLICIT attached-resource context:
   - sourcebound-service-not-exposure (correctly SourceArn+SourceAccount-bound
     service -> RESOURCE-CONFUSED-DEPUTY INFO negative control, never public)
   - same-account-scoped-grant-not-crossaccount (same-account SQS role grant ->
     RESOURCE-SAME-ACCOUNT-GRANT medium, not cross-account, not public)
   - private-bucket-named-principal-not-public (bucket policy naming one in-account
     user -> same-account grant, not PUBLIC-ACCESS)
   - kms-account-delegation-not-public-not-rootonly (KMS account-root delegation ->
     RESOURCE-KMS-ACCOUNT-DELEGATION, not public, not root-only, Resource:*=attached key)
   - org-constrained-star-not-critical ('*' narrowed by positive aws:PrincipalOrgID
     -> PUBLIC-ACCESS capped at HIGH, not unconditioned-critical)
   Wired into tests/negative-resource.test.js (mirrors negative-trust.test.js):
   present/absent/severity/exploitability caps, resource-only finding ids, the
   not-effective-access caveat on every finding, source-bound-not-missing guard,
   determinism.

2. Fixturized the resource-family suite tests as committed acceptance fixtures
   driven from analyze() (flip from fail-closed to real resource analysis):
   - suite-2 26/27/28/32/33/49/51/53 already live under fixtures/resource/ (from
     IAM-1201..1205, driven by tests/resource.test.js). Their blocked-by-design
     deferred fixtures were REMOVED from fixtures/acceptance-2-deferred/ (that set
     now holds only genuinely-unmodeled families: 30 boundary, 31 session, 43 SCP,
     52 RCP).
   - suite-3 69 + 85 added as new fixtures/resource/suite3-test-*.json (69 public
     '*' -> PUBLIC-ACCESS critical; 85 '*' narrowed by ArnLike aws:PrincipalArn ->
     PUBLIC-ACCESS high, condition-value wildcard preserved). Re-pointed
     tests/acceptance-suite-3-results.test.js MANIFEST 69/85 from BLOCKED ->
     COMPLETE_WITH_WARNINGS. The no-context fail-closed path (69) and identity-mode
     no-misflag guard (85) stay covered by their original dedicated fixtures/tests.
   - New tests/acceptance-resource-flip.test.js scoreboard: binds suite-2
     {26,27,28,32,33,49,51,53} + suite-3 {69,85} to their committed fixtures,
     asserts each analyzes (not fail-closed) with the expected resource finding,
     and asserts the remaining deferred set is exactly {30,31,43,52}.
   - Scoreboards updated: docs/acceptance-suite-2-results.md (8 rows flipped to
     pass + tally), docs/acceptance-suite-3-results.md (69/85 rows), new
     docs/acceptance-suite-2-resource-results.md (resource-results doc).

Gates: full node --test 1358 pass / 0 fail (was 1356; +2 net after removing 8
deferred subtests and adding negative-resource/flip/2-fixture subtests);
gate:no-network + gate:no-unsafe-dom exit 0; NO shipped HTML/JS/CSS changed
(shipped-tree-hygiene green). No regression to suites 1/2/3 or the identity/trust
negative corpora. Only remaining fail-closed set = genuinely-unmodeled shapes
(NotPrincipal hazard 29, RCP 52, SCP 43, boundary/session 30/31, unmodeled
services, missing context).

## Phase 12 close-out + IAM-1204 hold (2026-08-24)
7/8 accepted; resource family works (Suite-2 flips 8/8, Suite-3 69/85 pass, node
--test 1358/0, shipped-tree-hygiene clean - the Phase-11 gate held, no leak).
IAM-1204 HELD by the fail-closed arbiter on real adversarial findings in the
resource evaluator (from the IAM-1202/1203 principal + source-binding logic, NOT
1204's own same-account/S3-typing logic, which the critic verified SOUND):
  F1  Principal:"*" narrowed by aws:PrincipalArn / aws:PrincipalTag reported as
      critical ANONYMOUS/public (tests 49, 85) - the condition blocks anonymous;
      PRINCIPAL_SCOPING_KEYS omits PrincipalTag/userid/PrincipalOrgPaths.
  CD/N1 multi-value SourceArn/SourceAccount mismatch (disjoint account SETS) not
      detected -> wrongly "source-bound" info instead of "mismatched" medium.
  N2  analyzeResource(rejected-context) returns ok:true + "context recorded" note
      instead of failing closed (ok:false / RESOURCE_CONTEXT_REQUIRED).
  984 unmodeled principal types (CanonicalUser, wildcard-ARN, service-wildcard)
      silently dropped -> 0 findings; breaks "fail closed toward surfacing".
RESOLUTION: fold into IAM-1208 (resource-family adversarial hardening), run before
Phase 13. Core suite unaffected; these are edge over/under-claims + a fail-open.
