# OSV-Scanner accepted advisories.
#
# These are ALL dev-only transitive dependencies (via @playwright/test /
# @stryker-mutator/core) in tools/iam-blast-radius/package-lock.json. The SHIPPED
# package has ZERO runtime dependencies (architecture invariant), so none of these
# can reach a consumer's install or runtime - they affect only this repo's own
# CI/test environment. Dependabot (`.github/dependabot.yml`) tracks the dev tree
# and will open bump PRs; REMOVE each entry here once the corresponding advisory is
# resolved by a merged bump. Never add a runtime-dependency advisory to this file.

[[IgnoredVulns]]
id = "GHSA-2g4f-4pwh-qvx6"   # ajv (dev, via a test/mutation tool) - prototype pollution
reason = "dev-only transitive; shipped artifact is zero-dependency. Dependabot-tracked."

[[IgnoredVulns]]
id = "GHSA-4x5r-pxfx-6jf8"   # @babel/core (dev) - inefficient RegExp
reason = "dev-only transitive; shipped artifact is zero-dependency. Dependabot-tracked."

[[IgnoredVulns]]
id = "GHSA-52f5-9888-hmc6"   # tmp (dev, via @stryker) - symlink dir write
reason = "dev-only transitive; shipped artifact is zero-dependency. Dependabot-tracked."

[[IgnoredVulns]]
id = "GHSA-ph9p-34f9-6g65"   # tmp (dev, via @stryker) - arbitrary file/dir write via symlink
reason = "dev-only transitive; shipped artifact is zero-dependency. Dependabot-tracked."
